"""Lichtspiel ML / retrieval sidecar.

v0.1 implements deterministic metadata retrieval (Phase 5) with zero heavy
dependencies. MIDI/audio descriptors (Phase 6) and pretrained embeddings
(Phase 7) layer on top behind the same `retrieve()` interface.
"""

__version__ = "0.1.0"
