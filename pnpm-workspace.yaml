packages:
  - 'apps/p5-runtime'
  - 'apps/live-bridge'
  - 'apps/desktop'
  - 'packages/*'
allowBuilds:
  esbuild: true

# apps/ml-service is a Python project (uv / venv) and is intentionally
# excluded from the pnpm workspace.
