# converted-p5

Landing area for future **semi-automated** Processing→p5 conversions (the
migration-CLI direction from the Windchime animation work, if revisited).

Currently empty: all five v0.1 templates were hand-written against the
`VisualTemplate` contract in `apps/p5-runtime/src/templates/`, drawing concepts
(not code) from the Processing corpus — see `../source-processing/README.md`.
